# 共生之缚 游戏 Web 版镜像（nginx 静态托管）
# 构建前先运行 scripts/export_web.ps1 生成 client/build/web/（需 Godot 及导出模板）
FROM nginx:alpine

COPY deploy/nginx.conf /etc/nginx/conf.d/default.conf
COPY client/build/web /usr/share/nginx/html
