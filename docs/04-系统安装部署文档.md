# 04 系统安装部署文档

> 课程交付 4（含 docker 镜像文件）。撰写要点：环境要求、一键部署、逐服务说明、故障排查。

## 1. 部署架构
（docker-compose 拓扑 + 端口表）

## 2. 环境要求
- Docker Desktop（Windows）/ docker compose
- （桌面版运行）Godot 4.5.1 导出 Windows 版 或 直接编辑器运行

## 3. 一键部署（docker）
```powershell
docker compose -f deploy/docker-compose.yml up -d
```
| 服务 | 地址 | 说明 |
|---|---|---|
| game-web | http://localhost:8080 | 游戏 Web 版（需先 export_web） |
| server | http://localhost:8000/docs | 后端 + OpenAPI |
| mysql | 127.0.0.1:3306 | MySQL 8，自动执行 schema/seed |

## 4. 镜像构建说明
（Dockerfile.server / Dockerfile.game-web / 导出 Web 的构建顺序与依赖）

## 5. 本地开发部署
（setup.ps1 → build/export 数据 → 编辑器运行；run_server.ps1）

## 6. 验证与故障排查
（healthcheck、日志、常见问题：3306 占用 / Web 导出缺失 / 网络离线）

## 7. 发布产物清单
（导出包 / 镜像 / 数据库脚本位置）

## 8. 变更记录
