# 03 系统详细设计

课程交付 3。撰写要点：架构图、模块设计、数据结构（ER）、关键算法、时序图。

## 1. 总体架构
（README 架构图放大版：client / server / database / deploy 四层 + 数据流）

## 2. 客户端设计
### 2.1 场景树与模块划分
（scenes: main/world/player/entities/npc/items/fx/ui；scripts 镜像）
### 2.2 Autoload 设计
（EventBus 总线时序、GameManager 状态机图：BOOT→MAIN_MENU→PLAYING→PAUSED→ENDING/GAME_OVER）
### 2.3 组件设计（client/components/）
（state_machine / health / damageable / hookshot / inventory / dialogue —— 职责、信号、依赖）
### 2.4 关键机制设计
（玩家移动手感参数表、攻击判定（Area2D 框架）、钩锁物理、献祭流程时序、结局结算逻辑）

## 3. 数据设计
### 3.1 ER 图（服务器 MySQL）
（users 1—N saves / events / leaderboard；附 mermaid erDiagram）
### 3.2 游戏内容库（SQLite）
（items/enemies/npc/dialogues/endings/sacrifices 字段说明 + 关系）
### 3.3 数据管道时序
（改 SQL → build → export → DataDB → 运行时的完整时序图）

## 4. 服务器设计
（FastAPI 分层 router→service→repository→SQLAlchemy；token 方案；埋点批量写入；Redis 排行榜缓存（可选））
### 4.1 接口时序
（客户端登录→推存档→批量埋点的时序图）

## 5. 部署设计
（docker-compose 拓扑：mysql8/server/game-web(nginx)；healthcheck；首次启动自动建表）

## 6. 测试设计
（GUT/无头冒烟、pytest 分层：单元/接口/管道；CI 三个 job 说明）

## 7. 决策记录
（引用 docs/design/决策日志.md D-001~D-007；WebSocket 不做理由详述）

## 8. 变更记录
