# 02 系统接口设计文档

课程交付 2。本文件是**唯一接口权威**；实现若偏离必须同步此文档。素材：客户端接口见下方；REST 接口以 `/openapi.json` 为准。

## 1. 接口总览
（客户端内部 / 客户端↔服务器 REST / 数据管道 / 存档格式 四大类）

## 2. 客户端内部接口

### 2.1 Autoload 单例
| 单例 | 职责 | 关键 API |
|---|---|---|
| EventBus | 事件总线 | 信号（下表）+ `log_event(name, data)` |
| GameManager | 全局状态机 | `start_new_game()/record_sacrifice()/set_flag()/add_monster_limb()/try_consume_monster_limb()/to_dict()/from_dict()`（肢体持有数 `monster_limb_count`，M1-04） |
| DataDB | 数据访问 | `get_item/get_enemy/get_npc/get_dialogue/get_ending/get_sacrifice/table()` |
| SaveSystem | 存档 | `save(slot)/load(slot)/has_save(slot)`（Schema v1） |
| Settings | 设置 | `load_settings()/save_settings()` |
| Network | 网络 | `configure(base,token)/report_event()/flush_events()/push_save()/pull_save()` |

### 2.2 EventBus 信号表
（player_hurt / enemy_killed / item_picked / sacrifice_done / body_part_modified / save_loaded / save_failed / dialogue_started / dialogue_finished / ending_reached / game_over …… 与 `client/autoload/event_bus.gd` 保持同步）

### 2.3 组件接口（client/components/）

已定稿：
- **dialogue.gd**（M1-03 / M1-08）：`play(dialogue_id)` 播放（从 DataDB 读台词，禁止写死）、`is_playing()`；K 键快进/逐条；`next_id` 顺序链；信号 `dialogue_started(入口id)` / `dialogue_finished(入口id)` 成对广播；UI 契约 `show_box()/hide_box()/set_speaker()/set_text()/set_continue_hint(show)`（"按 K 继续"提示开关，见 dialogue_box）。
- **tutorial.gd**（M1-08 / M1-04）：新手教程状态机（对话结束后开始：移动→跳跃→攻击→下砍→战斗→捡肢体→献祭→完成）；复用 `dialogue_finished` / `enemy_killed` / `item_picked("monster_limb")` / `sacrifice_done` 信号推进；UI 契约 `show_hint(text)/hide_hint()`（见 tutorial_hint）。
- **altar.gd**（M1-04，scenes/items/altar.tscn）：祭坛献祭。玩家在范围内显示"按 K 献祭"；`try_sacrifice() -> bool` 消耗 1 肢体并 `GameManager.record_sacrifice("generic")`（无肢体返回 false 并提示）；对话期间（玩家 `_input_locked`）不响应 K，防与对话抢键。

待定稿：state_machine / health / damageable / hookshot / inventory —— M2 起逐个定稿。

### 2.4 场景切换协议
`SceneChanger.change_scene(path, params)` —— M1 定稿

## 3. 客户端 ↔ 服务器 REST API（v1）

| 方法 | 路径 | 说明 | 状态 |
|---|---|---|---|
| GET | /api/v1/health | 探针 | M0 ✅ |
| POST | /api/v1/auth/register | 注册 | M2 |
| POST | /api/v1/auth/login | 登录，返回 token | M2 |
| GET | /api/v1/saves/{slot} | 拉远程存档 | M2 |
| PUT | /api/v1/saves/{slot} | 推远程存档 | M2 |
| POST | /api/v1/events | 批量埋点 | M2 |
| GET | /api/v1/leaderboard | 排行榜 | M2 |

鉴权：`Authorization: Bearer <token>`。错误码约定：`{"detail": "..."}`（FastAPI 默认）。
WebSocket：**不做**（单机无实时需求，见 docs/03 §决策记录）。

## 4. 数据管道接口
`database/game_data/*.sql` → `build_game_data.py` → `.db` → `db_export.py` → `client/data/<表>/<id>.json` + `<表>_index.json`。
表清单：items/enemies/npc/dialogues/endings/sacrifices。字段以 `database/game_data/schema.sql` 为准。

## 5. 存档 Schema v1
```json
{
  "version": 1,
  "saved_at": "2026-...",
  "game_progress": { "sacrifice_count": 1, "current_floor": "floor_1", "faction": "", "flags": {} },
  "player": {},
  "inventory": {},
  "levels": {}
}
```
（player/inventory/levels 在 M1/M2 定稿并同步此文档）

## 6. 变更记录

| 日期 | 接口 | 变更 | 涉及任务卡 |
|---|---|---|---|
| M1 | dialogue UI 契约 | 追加 `set_continue_hint(show)`（"按 K 继续"提示开关） | M1-08 |
| M1 | tutorial 组件 | 新增 `tutorial.gd`：UI 契约 `show_hint(text)/hide_hint()`；复用 dialogue_finished/enemy_killed | M1-08 |
| M1 | GameManager | 新增 `monster_limb_count` + `add_monster_limb()/try_consume_monster_limb()`（拾取记账/献祭消耗）；序列化含该字段 | M1-04 |
| M1 | tutorial 组件 | 状态机追加 捡肢体→献祭 两步（复用 item_picked/sacrifice_done 推进） | M1-04 |
| M1 | altar 组件 | 新增 `altar.gd`：`try_sacrifice()`；玩家在范围内按 K 献祭（对话锁输入时忽略）；占用 world-space Label 提示 | M1-04 |
