# 决策日志（Decision Log）

记录影响架构/范围/流程的关键决策及理由。任何"为什么这样做"都能在这里找到答案。

| 日期 | 编号 | 决策 | 理由 | 提出人 |
|---|---|---|---|---|
| M0 | D-001 | 范围采用**方案 B**（游戏 + FastAPI+MySQL 后端 + docker 全栈） | 课程以技术评审为主；B 的后端/接口/数据库/docker 技术面与老师其他项目对齐，游戏仍为核心；C 工作量过大（上次混乱根源之一） | 技术主力 |
| M0 | D-002 | 引擎锁定 **Godot 4.5.1**，纯 GDScript，禁 C# | 全组统一版本避免漂移；GDScript 免构建链，agent 与新手友好 | 技术主力 |
| M0 | D-003 | 游戏静态数据走 **SQLite → JSON 管道**，运行时 Godot 只读 JSON，不引入 SQLite GDExtension | 满足"数据库脚本"交付 + 数据驱动架构；避免 GDExtension 版本兼容风险 | 技术主力 |
| M0 | D-004 | 服务器数据库 **MySQL 8**（docker 内置，首次启动自动执行 schema/seed） | 对齐老师技术栈（MySQL）；docker-compose 演示即部署 | 技术主力 |
| M0 | D-005 | **WebSocket 不做**，客户端↔服务器仅 REST | 单机无实时需求；避免无意义复杂度，接口文档中说明该取舍 | 技术主力 |
| M0 | D-006 | 网络层**离线优先**（事件入队、失败补发），无网完全可玩 | 演示兜底；后端故障不影响游戏验收 | 技术主力 |
| M0 | D-007 | 仓库**保留 git 历史**，重置以普通 commit 完成，不做 force push | 不破坏队友 clone；旧代码仍可从历史找回 | 技术主力 |

格式：新增条目请复制表格行，填写 日期/编号(D-xxx)/决策/理由/提出人。设计类决策（剧情/数值）见 GDD 变更记录。
