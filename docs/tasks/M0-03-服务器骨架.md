# M0-03 服务器骨架（FastAPI + /health + OpenAPI + 测试）

- 编号：M0-03 ｜ 指派：agent
- 输入：`server/README.md`；`database/schema.sql`（模型对齐）；`docs/02`（REST 约定 v1）
- 改动范围：`server/app/`、`server/tests/`、`server/requirements.txt`、`server/pytest.ini`
- 实现要求：
  1. `app/main.py`：FastAPI 实例，挂载 `/api/v1/health`，标题/描述含"共生之缚"
  2. `app/config.py`：pydantic-settings，`SYMBIONT_` 前缀
  3. `app/db.py` + `app/models.py`：SQLAlchemy 会话与 4 张表模型（users/saves/events/leaderboard），与 schema.sql 一致
  4. 测试：health、root 元信息、openapi 含路由
- 验收标准：`python -m pytest server -q` 全绿；`uvicorn app.main:app` 后 `GET /api/v1/health` 返回 ok；`/docs` 可访问
- 禁止：实现业务接口（auth/saves/events 属 M2）；改 `database/schema.sql` 不同步模型
- 产出：骨架 + 测试 + 完成报告
