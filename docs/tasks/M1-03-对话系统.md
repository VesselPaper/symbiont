# M1-03 对话系统（寄生体低语）

- 编号：M1-03 ｜ 指派：待认领（agent）
- 依赖：M1-01
- 输入：`database/game_data/dialogues`（intro_parasite_1/2 数据已就位）、`docs/02`、GDD §3
- 改动范围：`client/components/dialogue.gd`、`client/scenes/ui/dialogue_box.*`、`client/scripts/ui/`、必要时 `client/autoload/event_bus.gd`
- 实现要求：
  1. 对话数据从 DataDB 读（get_dialogue(id)），lines 是 JSON 数组 [{speaker,text}]
  2. 打字机字幕效果，按 interact(K) 快进/下一条，播完 emit dialogue_finished
  3. 出生地触发 intro_parasite_1 → intro_parasite_2（顺序链 next_id）
  4. UI：底部对话条 + 说话人名（中文字体 Noto Sans CJK 已入库）
- 验收标准：
  - [ ] 无头启动无报错，smoke_test 通过
  - [ ] F5：出生自动弹出寄生体对话，能打字机显示、按键逐条
- 禁止：实现分支/选项（M2）；写死台词（必须走 DataDB）
- 产出：代码 + 完成报告
