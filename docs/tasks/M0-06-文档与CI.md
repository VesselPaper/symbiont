# M0-06 文档初始化（00-07 模板 / GDD / 任务卡 / 周报 / CI）

- 编号：M0-06 ｜ 指派：agent
- 输入：`doc/老师发的/requirements.txt`（课程要求）；`docs/00`（本项目计划）
- 改动范围：`docs/`、`.github/workflows/ci.yml`
- 实现要求：
  1. `docs/01`–`docs/07` 按老师 7 项交付物建大纲模板（每份含"撰写要点"）
  2. `docs/design/`：GDD 框架（含 8 项待决策清单）、决策日志 D-001~D-007
  3. `docs/tasks/`：任务卡模板 + M0 任务卡（M0-01~M0-06）
  4. `docs/weekly/`：周报模板（对齐老师 xlsx：进度/分工/问题/下周计划）
  5. CI：data-pipeline（build+export+pytest tools）、server（pytest）、client（下载 Godot 4.5.1 → import → 冒烟启动 → smoke_test）
- 验收标准：
  - [ ] 7 份文档大纲齐全
  - [ ] CI 三个 job 在 GitHub 上跑绿（push 后验证）
  - [ ] GDD 待决策清单 8 项完整
- 禁止：让 agent 决定设计问题（清单留给 2 组拍板）
- 产出：文档体系 + CI + 完成报告
