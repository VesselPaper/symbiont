# M1-07 "拒绝献祭即死"结局

- 编号：M1-07 ｜ 指派：待认领（agent）
- 依赖：M1-03（对话）、M1-04（献祭流程）
- 输入：`database/game_data/dialogues`（refuse_die）、`database/game_data/endings`（refuse_die 数据已就位）、GDD §5
- 改动范围：`client/scenes/ui/ending.*`、`client/scripts/ui/`、`client/scenes/main/main.tscn`（结束回主菜单）、必要时 `client/autoload/game_manager.gd`
- 实现要求：
  1. 新手教学对话处提供"拒绝献祭"出口（interact 之外的按键，如按 Esc 或对话选项简版：按 1 献祭 / 按 2 拒绝）
  2. 拒绝 → 播放 refuse_die 台词 → 黑屏 → emit ending_reached("refuse_die") → 结算画面（显示结局名 + "重新开始"按钮回主菜单）
  3. 结局名从 DataDB.get_ending("refuse_die") 读取，不写死
- 验收标准：
  - [ ] 无头启动无报错，smoke_test 通过
  - [ ] F5：走完新手教学，选"拒绝"，能看到拒绝结局结算并回主菜单
- 禁止：实现其他结局（M3）；把结局文案写死
- 产出：代码 + 完成报告
