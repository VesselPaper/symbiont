# M1-08 对话"按K继续"提示 + 新手教程

- 编号：M1-08 ｜ 指派：agent
- 依赖：M1-03（对话系统）、M1-01（玩家动作）
- 输入：`client/components/dialogue.gd`、`client/scripts/ui/dialogue_box.gd`、`client/scripts/world/test_arena.gd`、`client/autoload/event_bus.gd`（enemy_killed 信号）
- 改动范围：`client/components/dialogue.gd`（加提示开关）、`client/scripts/ui/dialogue_box.gd` + `dialogue_box.tscn`（加"按K继续"提示 Label）、`client/components/tutorial.gd`（新建）、`client/scenes/ui/tutorial_hint.*` + `client/scripts/ui/tutorial_hint.gd`（新建，教程提示条）、`client/scenes/world/test_arena.tscn/gd`（挂教程、衔接对话结束）
- 实现要求：
  1. 对话 UI：当前条**完整显示后**在对话框角落出现"按 K 继续"小提示；打字中不显示；进入下一条时隐藏
  2. 新手教程 tutorial.gd：**寄生体对话结束（dialogue_finished(intro_parasite_1)）后才开始**，动作触发式逐步提示（屏幕上方提示条）：
     - ① 移动："按 A/D 左右移动" → 玩家产生水平移动后下一步
     - ② 跳跃："按 空格 跳跃" → 玩家跳起后下一步
     - ③ 攻击："按 J 攻击" → 玩家挥砍后下一步
     - ④ 下砍："空中下落时按 J 下砍" → 玩家触发下砍后下一步
     - ⑤ 战斗："有敌人靠近！按 J 攻击它" → 击杀任意敌人后教程结束
  3. 提示条：屏幕上方半透明提示 + 中文（Noto Sans CJK 字体），出现后直到满足条件才切换
  4. 动作检测：读 Input（move/jump/attack）+ 读玩家 `_pogo_active` + 监听 EventBus.enemy_killed；玩家节点用 get_first_node_in_group("player")
  5. 教程提示文案可以写在代码常量里（UI 文案，非游戏内容数据，M1 简化）；但对话台词仍走 DataDB 不许写死
- 验收标准：
  - [ ] `--headless --quit-after 60` 无报错；smoke_test 退出码 0
  - [ ] F5：寄生体对话播完出现"按K继续"提示；对话结束后教程逐步出现，按 A/D、空格、J、下砍、击杀史莱姆 可依次推进到最后并结束
- 禁止：改 player.gd 核心逻辑（只读它）；改 docs/database/tools/server/smoke_test；写死对话台词
- 产出：代码 + 完成报告（含教程步骤、F5 测试方法）
