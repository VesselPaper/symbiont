# 脚手架清单（Scaffold Checklist）

脚手架 = 项目骨架。对应老师本周要求的"脚手架"。以下每一项都已搭好并验证过，供验收对照。

## 1. 仓库与版本

- [x] GitHub 仓库 https://github.com/VesselPaper/symbiont（公开，可克隆）
- [x] main 分支；已开启分支保护（禁止直接 push，走 PR + review + CI）
- [x] .gitignore / .editorconfig / .gitattributes 就位
- [x] CI（GitHub Actions）全绿：数据管道 + 后端测试 + Godot 无头冒烟，最近 10 次运行全部通过

## 2. 客户端（Godot 4.5.1）

- [x] project.godot：1280x720、GL Compatibility、主场景就位
- [x] 6 个 autoload 单例：EventBus / GameManager / DataDB / SaveSystem / Settings / Network
- [x] 主场景可运行：F5 看到 M0 标题页
- [x] 无头冒烟测试通过（client/tests/smoke_test.gd）
- [x] 目录结构：scenes / scripts / components / data / assets / tests
- [x] 素材：Kenney 地牢瓦片 + UI 边框 + 中文字体（CC0/OFL，CREDITS 登记）

## 3. 数据管道

- [x] database/game_data：SQLite 6 表（物品/敌人/NPC/对话/结局/献祭），schema + seed
- [x] tools/build_game_data.py + tools/db_export.py：SQL → JSON → client/data
- [x] 运行验证：6 表 23 条导出成功，pytest 通过

## 4. 后端（FastAPI）

- [x] 骨架：/health 接口 + OpenAPI 文档（/docs）+ SQLAlchemy 模型（4 表）
- [x] 测试：pytest 通过，health 返回 200
- [x] 数据库脚本：database/schema.sql、database/seed.sql（MySQL 8）

## 5. 部署（docker）

- [x] docker-compose：mysql8 + server + game-web(nginx) 编排文件
- [x] Dockerfile.server / Dockerfile.game-web / nginx.conf
- [ ] game-web 镜像实际构建（依赖 Godot Web 导出，M2 完成）—— 唯一未完成项，不阻塞本周

## 6. 文档

- [x] docs/00~07 课程文档大纲
- [x] GDD、决策日志、任务卡模板、周报模板
- [x] 标准与规范.md（本周完成）
- [x] 仓库联通检查表.md（本周完成）

## 结论

脚手架主体完成；未完成项只有 game-web 镜像构建（M2 需要 Web 导出），不阻塞后续开发。当前状态：可以开始 M1 玩法开发。
