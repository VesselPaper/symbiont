# scripts/ — 开发工具脚本（规划）

随开发补全，计划包含：

- `export_webgl.ps1` / `.sh` — Godot 无头导出 WebGL 到 `client/build/`
- `db_migrate.sh` — 执行 Alembic 迁移
- `db_seed.sh` — 导入 `database/seed.sql`
- `dev_up.sh` — 一键启动本地开发环境（后端 + 依赖）
